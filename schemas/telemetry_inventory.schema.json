{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://oah.dev/schemas/telemetry_inventory.schema.json",
  "title": "Existing telemetry inventory (S2 output)",
  "description": "What already exists, per architecture.md S2: loggers and call sites, metrics libraries, existing OTel/vendor SDK usage, correlation-ID mechanics, error handling that swallows or surfaces failures.",
  "type": "object",
  "required": ["schema_version", "repo", "generated_by"],
  "properties": {
    "schema_version": { "const": "0.1.0" },
    "repo": {
      "type": "object",
      "required": ["path", "git_sha"],
      "properties": {
        "path": { "type": "string" },
        "git_sha": { "type": "string" }
      }
    },
    "generated_by": {
      "type": "object",
      "required": ["harness_version"],
      "properties": {
        "harness_version": { "type": "string" }
      }
    },
    "loggers": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "file", "line", "level", "logger_kind"],
        "properties": {
          "id": { "type": "string" },
          "file": { "type": "string" },
          "line": { "type": "integer", "minimum": 1 },
          "symbol": { "type": "string" },
          "level": { "enum": ["debug", "info", "warning", "error", "critical", "exception"] },
          "logger_kind": { "enum": ["stdlib_logging", "custom_wrapper", "print"] },
          "wrapper_module": { "type": "string", "description": "e.g. beacon_logging, when logger_kind is custom_wrapper" }
        }
      }
    },
    "existing_otel_usage": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "file", "line", "package"],
        "properties": {
          "id": { "type": "string" },
          "file": { "type": "string" },
          "line": { "type": "integer", "minimum": 1 },
          "package": { "type": "string", "description": "e.g. opentelemetry, opentelemetry.trace" }
        }
      }
    },
    "metrics_libraries": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "file", "line", "library"],
        "properties": {
          "id": { "type": "string" },
          "file": { "type": "string" },
          "line": { "type": "integer", "minimum": 1 },
          "library": { "enum": ["prometheus_client", "statsd", "datadog", "ddtrace"] }
        }
      }
    },
    "error_handling": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "file", "line", "pattern"],
        "properties": {
          "id": { "type": "string" },
          "file": { "type": "string" },
          "line": { "type": "integer", "minimum": 1 },
          "symbol": { "type": "string" },
          "exception_type": { "type": "string" },
          "pattern": {
            "enum": ["swallowed", "logged", "reraised"],
            "description": "swallowed: except body neither logs nor re-raises. logged: calls a detected logger. reraised: contains a raise statement."
          }
        }
      }
    },
    "vendor_dependencies": {
      "type": "array",
      "description": "Manifest-declared (package.json), not source-confirmed -- a separate, weaker evidence tier from existing_otel_usage's source-level import scan. A declared dependency shows the repo has some telemetry vendor wired up somewhere, not that it's actually imported/used.",
      "items": {
        "type": "object",
        "required": ["id", "file", "line", "vendor", "package", "category"],
        "properties": {
          "id": { "type": "string" },
          "file": { "type": "string", "description": "The manifest the dependency was declared in, e.g. package.json" },
          "line": { "type": "integer", "minimum": 1 },
          "vendor": {
            "enum": ["opentelemetry", "dynatrace", "new_relic", "splunk", "datadog", "sentry",
                     "winston", "pino", "prometheus", "statsd"]
          },
          "package": { "type": "string", "description": "The exact declared npm package name, e.g. @opentelemetry/sdk-node" },
          "category": { "enum": ["apm_tracing", "error_tracking", "logging", "metrics"] }
        }
      }
    },
    "summary": {
      "type": "object",
      "properties": {
        "files_scanned": { "type": "integer" },
        "logger_call_sites": { "type": "integer" },
        "swallowed_exceptions": { "type": "integer" },
        "has_existing_otel": { "type": "boolean" },
        "vendor_dependencies_count": { "type": "integer" },
        "has_commercial_apm": {
          "type": "boolean",
          "description": "True if a non-OpenTelemetry APM vendor (Dynatrace, New Relic, Splunk, Datadog) is a declared dependency."
        }
      }
    }
  }
}
