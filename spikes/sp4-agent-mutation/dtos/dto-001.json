{
  "dto_id": "dto-001",
  "target_file": "agents/intake.py",
  "insertion_point": {"near_line": 56, "anchor": "response = client.messages.create("},
  "instrumentation_type": "wrap_llm_call_with_telemetry_emit",
  "code_shape_precondition": "a direct client.messages.create(...) call assigned to `response`",
  "instructions": "Import oah_telemetry at the top of the file if not already imported. Immediately before the `response = client.messages.create(` call, add a line calling oah_telemetry.emit(\"gen_ai.request.start\", model=INTAKE_MODEL). Immediately after the call (once `response` is assigned), add oah_telemetry.emit(\"gen_ai.request.end\", model=INTAKE_MODEL). Do not change anything else in the file -- no reformatting, no renaming, no touching other functions.",
  "expected_emitted_events": ["gen_ai.request.start", "gen_ai.request.end"]
}
